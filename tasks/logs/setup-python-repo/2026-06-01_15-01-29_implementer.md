# Implementer log — setup-python-repo

**Timestamp:** 2026-06-01_15-01-29
**Role:** Implementer
**Card:** tasks/doing/setup-python-repo.md

## What the card asked

Foundation scaffold: a uv-managed Python project (`pyproject.toml`, lockfile,
Python 3), a package directory for app code, a trivial runnable entry so `uv run`
proves the toolchain, and pytest as a dev dependency so `uv run pytest` works.
No app features — just the scaffold.

## Note on paths

The task instructions referenced the board as `workflow/...`, but the real board
in this repo lives under `tasks/` (todo/doing/logs). I worked the actual `tasks/`
structure. Card was found at `tasks/todo/setup-python-repo.md`.

## Homework

- `uv 0.9.17` and multiple CPython interpreters are installed (3.12–3.14).
- Read the sibling cards to keep the layout compatible:
  - `todo-store.md` expects a module named `todo_store` to be added.
  - `todo-app.md` expects an `app` script wired into `pyproject.toml`
    (`uv run app`). I deliberately did NOT claim the `app` script name — that
    belongs to the GUI card. I named the scaffold's entry `hello` so the GUI card
    can add `app` later without collision.

## What I built

- `pyproject.toml` — project metadata, `requires-python = ">=3.12"`, empty
  runtime deps, a `[project.scripts]` entry `hello = "todo_app.main:main"`,
  pytest in `[dependency-groups].dev`, hatchling build backend pointed at
  `src/todo_app`, and `[tool.pytest.ini_options] testpaths = ["tests"]`.
- `src/todo_app/__init__.py` — package marker (src layout, clean place to drop
  `todo_store.py` and the GUI module).
- `src/todo_app/main.py` — `hello()` returns a health-check string; `main()`
  prints it.
- `tests/test_smoke.py` — one trivial test asserting on `hello()`.
- `.gitignore` — excludes `.venv/`, caches, build artifacts.
- `uv.lock` — generated by `uv sync`.

## Verification (no foreground blocking)

- `uv sync` — resolved 7 packages, created `.venv`, installed pytest. OK.
- `uv run hello` -> "Hello from todo_app! The uv toolchain works." OK.
- `uv run python -m todo_app.main` -> same output. OK.
- `uv run pytest` -> 1 passed, no config errors. OK.

All three "Done when" conditions met. No server or GUI involved, so nothing to
background/stop.

## Notes / out-of-scope (not acted on)

- Scaffold entry script is named `hello`, leaving `app` free for the GUI card.
- `uv sync` selected CPython 3.14 (satisfies `>=3.12`).
- Left the card in `doing/` for the reviewer.
