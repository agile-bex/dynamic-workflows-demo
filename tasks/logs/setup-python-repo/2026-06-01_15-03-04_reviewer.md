# Reviewer log — setup-python-repo

**Timestamp:** 2026-06-01_15-03-04
**Role:** Reviewer
**Card:** tasks/doing/setup-python-repo.md
**Commit reviewed:** 968d2f7 "Scaffold uv Python project (setup-python-repo)"
**Verdict:** Approved

## What the card asked

A uv-managed Python project (pyproject.toml, lockfile, Python 3), a package
directory, a trivial runnable entry so `uv run` proves the toolchain, and pytest
as a dev dependency so `uv run pytest` works. Just the scaffold — no app features.

## Did it do what was asked (and only that)

Yes, and stayed in scope.

- `pyproject.toml` — `requires-python = ">=3.12"`, empty runtime deps, hatchling
  build backend pointed at `src/todo_app`, pytest in `[dependency-groups].dev`,
  and `testpaths = ["tests"]`. Clean.
- `uv.lock` committed.
- `src/todo_app/` package (src layout) with `__init__.py` marker — a clean place
  to drop later modules.
- `src/todo_app/main.py` — `hello()` / `main()` health-check entry, wired as the
  `hello` script.
- `tests/test_smoke.py` — one trivial passing test.
- `.gitignore` excludes `.venv/`, caches, build artifacts; confirmed no `.venv`
  or `.pytest_cache` files are tracked (`git ls-files` count = 0).
- No app/todo logic added. Scope respected.

Good judgment call: the entry script is named `hello`, deliberately leaving the
`app` name free for the GUI card (todo-app.md), avoiding a future collision. The
package name `todo_app` is compatible with the sibling cards' expectations.

## Real running result (verified by me)

- `uv run hello` -> "Hello from todo_app! The uv toolchain works." (exit 0)
- `uv run pytest` -> 1 passed, no config errors (configfile: pyproject.toml,
  Python 3.14.0).
- bare `uv run` -> exits 0 and lists `hello` among available commands.
- `README.md` (referenced by `readme` in pyproject) exists and is tracked.

All three "Done when" conditions met.

## Findings

None blocking. No nits worth padding the list with.

## Decision

Approved. Moving the card doing/ -> done/ in this commit.
