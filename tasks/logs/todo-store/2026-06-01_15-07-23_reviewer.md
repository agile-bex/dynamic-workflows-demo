# Reviewer — todo-store (round 1)

**Verdict: Approved.** Card moved `doing/ → done/`.

## What I reviewed

Commit `5704326` — `src/todo_app/todo_store.py`, `tests/test_todo_store.py`, the
implementer log, and the card move.

## Real running result

- `uv run pytest -q` → **14 passed** in 0.03s. This is the card's stated gate; green.
- `uv run python -c "import ...todo_store"` imports cleanly with **no PySide6/Qt**
  in `sys.modules`. UI-free constraint satisfied.

## Did it do what the card asked (and only that)

- CRUD present: `create`, `get`, `list`, `update`, `delete`. Todo carries
  `id` / `title` / `done`. Yes.
- Single `todos.json` owned by the store; `save`/`reload` round-trips. A missing,
  corrupt, or non-list file all load as `[]` rather than crashing. Yes (and the
  non-list case is exercised too).
- Tests are genuine, not hollow: they assert on returned fields, verify
  persistence via a *fresh* `TodoStore` over the same path (real round-trip), and
  cover update-missing/delete-missing/corrupt/non-list/missing-file branches.
- Every test uses `tmp_path`; the suite cannot touch a real `todos.json`. Constraint met.
- In scope: no GUI, no server. No scope creep.

## Findings

### Nits (non-blocking)

- `_load` keeps any `dict` entry but doesn't require an `id` key; a persisted dict
  missing `id` would later `KeyError` in `_next_id`/`_find`. Beyond the card's
  "corrupt loads as empty" cases, so not blocking — worth a guard if hardened later.
- `Todo = dict` is loose typing (no TypedDict). Acceptable at this scope.

Nothing blocking. Approving.
